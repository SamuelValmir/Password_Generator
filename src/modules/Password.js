export default class Password {
    constructor(number, uppercase, lowercase, symbols) {
        this.number = number;
        this.uppercase = uppercase;
        this.lowercase = lowercase;
        this.symbols = symbols;
    };

    generate(){

    };
}